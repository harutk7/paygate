apiVersion: apps/v1
kind: Deployment
metadata:
  name: landing
  namespace: paygate
  labels:
    app: landing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: landing
  template:
    metadata:
      labels:
        app: landing
    spec:
      containers:
        - name: landing
          image: localhost:5000/paygate-landing:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: landing
  namespace: paygate
  labels:
    app: landing
spec:
  type: NodePort
  selector:
    app: landing
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30210
      name: http
